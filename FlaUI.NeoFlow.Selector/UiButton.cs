using FlaUI.Core.AutomationElements;

namespace FlaUI.NeoFlow.Selector;

public class UiButton : UiElement {

    internal UiButton(AutomationElement element, SelectorEngine engine) : base(element, engine) {
    }
}
