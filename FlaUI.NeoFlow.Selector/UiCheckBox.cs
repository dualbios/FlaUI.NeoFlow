using FlaUI.Core.AutomationElements;

namespace FlaUI.NeoFlow.Selector;

public class UiCheckBox : UiElement {

    internal UiCheckBox(AutomationElement element, SelectorEngine engine) : base(element, engine) {
    }
}
