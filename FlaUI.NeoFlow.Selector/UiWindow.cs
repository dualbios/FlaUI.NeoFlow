using FlaUI.Core.AutomationElements;

namespace FlaUI.NeoFlow.Selector;

public class UiWindow : UiElement {

    internal UiWindow(AutomationElement element, SelectorEngine engine) : base(element, engine) {
    }
}
